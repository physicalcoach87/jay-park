# 연장전(ET) 데이터 구조 정의

작성: 2026-08-20
대상: `match_records` 테이블 / 코치 웹앱(`index.html`) / 코치 모바일앱(`coach-mobile/index.html`)

배경: 컵대회 연장전 발생. 기존 4분류(FT/1Q/2Q/Top)로는 연장전을 담을 자리가 없고,
FT 컬럼 하나가 ①부하 집계 ②능력 기준선 ③구간 분석 세 용도를 겸하고 있어 충돌이 발생함.

---

## 1. 확정 사항

### 1.1 time_type 체계

| time_type | 정의 | 성격 |
|---|---|---|
| `FT` | **정규 경기 전체** — 정규시간 + 주심 추가시간 (**연장 제외**) | 독립 |
| `ET` | **연장 전체** — 연장 전·후반 + 각 추가시간 (승부차기 제외) | 독립 — 가산 |
| `1Q` | 정규 전반 (추가시간 포함) | FT의 부분집합 — **합산 금지** |
| `2Q` | 정규 후반 (추가시간 포함) | FT의 부분집합 — **합산 금지** |
| `Top` | 경기 후 추가훈련 (미출전·교체 선수) | 독립 — 가산 |

핵심: **FT는 "정규 경기 한 판"을 뜻한다.** 90분이라는 숫자로 규정하지 않는다 —
추가시간을 포함한 정규 경기 전체가 FT이고, 연장은 여기서 빠진다.
연장을 FT에 넣지 않는 이유는 동일 기준으로 기록하는 것이 기준선의 생명이고,
연장전은 시즌 1~2회의 예외이기 때문. `1Q + 2Q = FT` 전제도 이로써 그대로 유지된다.

**연장은 `ET` 하나로 저장한다.** 연장 전·후반을 나누지 않는다.

> `duration` 주의: FT는 고정값이 아니다. 추가시간에 따라 **95분·110분·115분** 등으로 제각각 들어온다.
> **길이와 무관하게 전부 정상 FT이며, 전체 경기로 취급한다.** 상한을 두지 않는다.
> ET도 같은 이유로 길이가 가변이다. 코드에서 특정 숫자를 상수로 가정하거나 상한을 걸지 말 것.

### 1.2 용도별 합산 규칙

| 용도 | 합산식 | 근거 |
|---|---|---|
| Match Max · 개인 목표 기준선 | **FT만** | 동일 조건(정규 경기) 비교가 기준선의 전제. 연장은 예외로 제외 |
| 경기 보고서 | **FT + ET** | 경기에서 발생한 전체 부하. Top-up은 경기가 아니므로 별도 섹션 유지 |
| 개인 부하 (ACWR · 부상위험 · 회복 우선순위 · 컨디션) | **FT + ET + Top** | 선수가 실제로 받은 총 부하 |
| 전후반 낙폭 분석 (5분 bin, 롤링 mpm) | **1Q / 2Q**, ET는 별도 표기 | `1Q+2Q=FT` 전제 유지. ET를 2Q에 섞으면 왜곡됨 |
| **최근 3경기 평균** (팀 기준선 · 개인 목표 · 주기화 %) | **FT만** | 동일 조건 비교. 1.7 참조 |

### 1.3 지표별 합산 방식

여러 행을 합칠 때 지표 성격에 따라 방식이 다르다. **일괄 더하기는 오류를 만든다.**

| 방식 | 컬럼 |
|---|---|
| **더하기** | `duration` `td` `hir_sprint` `band3_td` `band4_td` `band5_td` `band4_n` `band5_n` `accel` `decel` `rhie` `player_load` `fmp_run_m` `fmp_run_h` `fmp_dyn_m` `fmp_dyn_h` |
| **최댓값** | `max_speed` |
| **재계산** | `mpm` = (합산 `td`) ÷ (합산 `duration`) |

`mpm`을 더하면 안 된다 — FT 110 + ET 95 = 205 같은 값이 나온다. 반드시 재계산.
`max_speed`는 시간 비례 지표가 아니므로 더하지 않고 최댓값을 취한다.

### 1.4 파일명 규칙

```
YYYY-MM-DD-AM        훈련 오전
YYYY-MM-DD-PM        훈련 오후
YYYY-MM-DD-M_FT      정규 경기 전체 (추가시간 포함)
YYYY-MM-DD-M_1Q      정규 전반
YYYY-MM-DD-M_2Q      정규 후반
YYYY-MM-DD-M_ET      연장 전체              ← 신규
YYYY-MM-DD-M_Top     경기 후 추가훈련
```

### 1.5 승부차기

**저장하지 않는다.** GK를 제외하면 부하 관점에서 의미가 없다.
카타펄트에서 `ET` 구간을 자를 때 연장 후반 종료 시점까지만 포함한다.

### 1.6 ET만 출전한 선수 (연장에만 투입된 교체 선수)

`FT` 행 없이 `ET` 행만 존재하는 선수. 다음과 같이 처리한다.

| 대상 | 처리 | 근거 |
|---|---|---|
| 경기 보고서 | **포함** — 출전 선수로 표시 | 실제로 경기에 뛴 선수 |
| 개인 부하 (ACWR · 부상위험 · 회복) | **포함** | 실제로 받은 부하 |
| Match Max · 개인 목표 기준선 | **제외** | 정규 경기 기록이 아님 |
| 팀 평균 · 전후반 낙폭 분석 | **제외** | 정규 경기 표본이 아님 |

대부분은 기존 구조에서 자동으로 성립한다 —
Match Max와 기준선은 `.eq('time_type','FT')`이므로 `ET`만 있는 선수는 애초에 걸리지 않고,
부하 집계는 화이트리스트에 `'ET'`를 넣는 것만으로 포함된다.

**신규 코드가 필요한 곳은 보고서 표시 하나뿐이다.**

구현 시 `Top-up만`처럼 **별도 그룹으로 빼지 않았다.** Top-up 선수는 경기에 뛰지 않았으므로
회복 우선순위에서 D그룹으로 분리하는 것이 맞지만, ET만 출전한 선수는 **실제로 경기에 뛴 선수**다.
따라서 다른 출전 선수와 동일하게 **출전시간(ET duration) 기준으로 A/B/C 그룹에 배정**하고,
ET 섹션 표에 `연장 투입` 배지를 달아 식별만 가능하게 했다.

### 1.7 최근 3경기 평균 — FT 전용 (감사 완료 2026-08-20)

팀 기준선·개인 목표·주기화 % 산정에 쓰이는 **모든 "최근 3경기 평균"은 `FT`만으로 계산한다.**
연장 경기라도 그 경기의 `FT` 행(정규 경기분)만 들어가고 `ET`는 절대 포함되지 않는다.

`ET`를 부하 집계 화이트리스트(3.2)에 추가할 때 3경기 평균으로 새어 들어갈 위험이 있어
전 지점을 감사했다. **10곳 모두 이미 FT 전용이며, `ET` 추가의 영향을 받지 않는다.**

| 3경기 평균 지점 | 데이터 출처 | FT 보장 방식 |
|---|---|---|
| `:10338` 홈 대시보드 `matchRef3` | `:10315` | 쿼리 `.eq('time_type','FT')` |
| `:13362` `window._matchRef3` | `:13347` | 쿼리 `.eq(...,'FT')` |
| `:14630` `:14743` `_ref3Rows` | `:14580` | 쿼리 `.eq(...,'FT')` |
| `:15308` `:15326` `personalMAvg` | `:15024` | 쿼리 `.eq(...,'FT')` |
| `:17755` `buildPersonalPlanTarget` | `pmatch` | 함수 내부 `time_type==='FT'` 필터 |
| `:17880` `deriveDemand` 팀 기준선 | `:17861` **(화이트리스트)** | `:17879`에서 `time_type==='FT'` 필터 |
| `:22851` 보고서 최근 3경기 팀 평균 | `:22655` | 쿼리 `.eq(...,'FT')` |
| `:25614` Match Max | `:25565` | 쿼리 `.eq(...,'FT')` |
| `:28917` 주기화 기준 스냅샷 | `:28913` | 쿼리 `.eq(...,'FT')` |
| `:29273` 블록 기준 백필 | `:29261` | 쿼리 `.eq(...,'FT')` |

수정 대상 화이트리스트 8곳 중 3경기 평균과 닿는 것은 `:17861` 하나뿐이며,
소비 지점(`:17879`)에 명시적 `FT` 필터가 있어 안전하다.

**향후 주의**: `:17861`의 `matchAll`은 `:17910`에서 `pmatch`로 넘어가 개인 부하 계산에 쓰인다.
`:17917`의 `mDur`는 날짜별 duration을 **합산**하므로 `ET` 추가 시 `FT+ET+Top` 합산이 되어
1.2의 개인 부하 규칙과 일치한다(의도된 동작). 반면 `:17941` `recovFtMatches`와
`:17952` `buildPersonalPlanTarget`은 FT 전용이므로 건드리지 말 것.

### 1.8 구조 선택의 부수 효과 (의도된 이득)

FT를 정규 경기로 한정하므로, Match Max 산정 쿼리(`index.html:25567`, `.eq('time_type','FT')`)와
개인 기준선(`index.html:17753`, `index.html:15324`)은 **코드 수정 없이** 자동으로 동일 기준을 유지한다.
연장 경기가 기준선을 영구 오염시키는 최대 위험이 구조 선택만으로 제거됨.

기존 필터 통과 여부도 문제없다 — Match Max의 `duration>=90`(`index.html:25596`)과
개인 기준선의 `duration>=80`은 추가시간 포함 FT(95~105분)를 정상적으로 통과시킨다.

---

## 2. 사전 확인 결과 · 잔여 항목

### 2.1 `time_type` CHECK 제약 — 없음 (확인 완료 2026-08-20)

Supabase SQL Editor에서 확인:
```sql
SELECT conname, pg_get_constraintdef(oid) AS def
FROM pg_constraint
WHERE conrelid = 'match_records'::regclass AND contype = 'c';
```
결과 **0행**. `match_records`에 CHECK 제약이 전혀 없으며 `time_type`은 자유 text 컬럼이다.
→ **`ET` 저장에 DB 마이그레이션이 필요 없다. 코드 변경만으로 완결된다.**

### 2.2 Match Max 상한 — 도입하지 않음 (결정 2026-08-20)

`index.html:25596`의 `duration>=90` 필터에 **상한을 걸지 않는다.**
A경기 110분 / B경기 115분 / C경기 95분이 모두 각자의 정규 경기 전체이며,
길이가 다르다는 이유로 어느 하나를 Match Max 산정에서 빼면 안 된다.
FT는 길이와 무관하게 FT로 계산한다.

(검토했던 `<=110` 상한안은 추가시간이 긴 정상 경기를 조용히 탈락시키므로 폐기.)

### 2.3 잔여 항목

| # | 항목 | 상태 |
|---|---|---|
| 1 | 카타펄트 절단 규칙 | FT = 킥오프~정규 종료(추가시간 포함) / ET = 연장 킥오프~연장 후반 종료(승부차기 제외). 운영 절차로 공유 필요 |

---

## 3. 구현 완료 (2026-08-20)

### 3.1 ET 입력 경로 (4곳)
- `index.html:16719` — 파일명 파싱 regex에 `ET` 추가
- `index.html:16951` — 업로드 프리뷰 time_type 드롭다운에 `ET` 추가
- `index.html:1100` — 경기 상세 "TIME 기준" select에 `ET (연장)` 추가
- `index.html:939` — 파일명 규칙 안내 문구에 `M_ET` 추가

### 3.2 부하 집계에 ET 합산 (화이트리스트 8곳)
`.in('time_type',['FT','Top','Top-up','TOP'])` → `'ET'` 추가
- `index.html:22979` — **ACWR chronic 계산 (최우선)**
- `index.html:5646` `index.html:10693` `index.html:11901` `index.html:15030`
  `index.html:15781` `index.html:15927` `index.html:17861`

주석도 갱신: `1Q/2Q`는 부분집합이라 제외, `ET`는 독립 가산이라 포함.

### 3.3 합산 로직 확장 (2항 → 3항)
- `index.html:11968` — 개인 운동부하: `FT + Top-up` → `FT + ET + Top`. 레이블도 `FT+ET+Top-up` 식으로
- `index.html:23052` — 경기 리포트 통합 맵에 `et` 추가
- 1.3의 지표별 합산 방식(더하기 / 최댓값 / mpm 재계산)을 반드시 준수

### 3.4 경기 보고서에 ET 반영
- `index.html:22648` / `index.html:25798` — `etRows` 분리 추가
- `index.html:24008` — Top-up 섹션과 나란히 ET 섹션 신설
- 보고서 헤더 출전시간·TD 총합을 `FT + ET`로 표시 (연장 경기일 때만)
- `index.html:23081` — `Top-up만` 그룹에 대응하는 **`ET만` 그룹 분류 신설** (1.6 참조)

### 3.5 기존 버그 동반 수정 (필수)
`index.html:18199` 부상위험 분석 쿼리가 `time_type`을 조회하지 않아,
경기일 duration을 **날짜별 첫 행**으로 잡는다(`index.html:18240`, `_pMatchDur`).
지금도 `1Q`가 뽑힐 수 있는 버그이며, ET 도입 후에는 연장 경기일 sRPE가
연장 구간만으로 계산될 수 있다. → `time_type` 조회 + `FT+ET+Top` 합산으로 수정.

### 3.6 수정 불필요 (구조 선택으로 자동 해결)
- `index.html:25567` Match Max — `.eq('time_type','FT')`, FT가 정규 경기이므로 그대로 정상
- `index.html:17753` / `index.html:15324` 개인 목표 기준선 — `duration>=80` 필터가 FT만 통과

### 3.7 코치 모바일앱 확인 필요
- `coach-mobile/index.html:661` `:1532` — `.eq('time_type','FT')` 용도 확인
- `coach-mobile/index.html:2429` — `filter(r=>(r.time_type||'FT')==='FT')`, 표시 목적이면 유지 가능
